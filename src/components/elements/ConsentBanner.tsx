'use client';

/**
 * Placeholder for cookie/consent UI.
 * This project currently doesn't implement a consent flow, but the architecture
 * expects a ConsentBanner component to exist.
 */
export default function ConsentBanner() {
  return null;
}


/**
 * Client-side store initialization hook.
 *
 * This project currently doesn't use Zustand, but the target architecture expects
 * a central place to kick off remote-loaded stores.
 */
export function initClientStores() {
  // no-op for now
}

